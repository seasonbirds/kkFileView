# 修改总结

## 1. 创建SsoConfig.java配置类
- 路径：`server/src/main/java/cn/keking/config/SsoConfig.java`
- 功能：管理单点登录的独立配置，包括是否启用、token名称、登录URL和Redis配置等
- 配置项：
  - `sso.enabled`：是否启用单点登录（默认：false）
  - `sso.tokenName`：登录校验的Token名称（默认：TOKEN）
  - `sso.loginUrl`：登录失败后跳转的URL（默认：/login）
  - `sso.redis.host`：Redis服务器地址（默认：localhost）
  - `sso.redis.port`：Redis服务器端口（默认：6379）
  - `sso.redis.password`：Redis密码（默认：空）
  - `sso.redis.database`：Redis数据库索引（默认：0）
  - `sso.redis.timeout`：Redis连接超时时间（毫秒，默认：3000）
  - `sso.redis.userPrefix`：用户信息存储的前缀（默认：USER-）

## 2. 修改SsoFilter.java
- 路径：`server/src/main/java/cn/keking/web/filter/SsoFilter.java`
- 功能：修改单点登录过滤器，使其使用SsoConfig配置类和独立的RedissonClient
- 修改内容：
  - 添加SsoConfig依赖
  - 移除RedissonClient依赖
  - 初始化独立的RedissonClient，使用SsoConfig中的Redis配置
  - 修改Token获取逻辑，使用SsoConfig中的tokenName
  - 修改Redis密钥前缀，使用SsoConfig中的userPrefix
  - 关闭RedissonClient的资源清理

## 3. 修改WebConfig.java
- 路径：`server/src/main/java/cn/keking/config/WebConfig.java`
- 功能：修改Web配置，使其使用SsoConfig配置类
- 修改内容：
  - 将getSsoFilter方法的参数从RedissonClient改为SsoConfig
  - 向SsoFilter注入SsoConfig而不是RedissonClient

## 4. 修改pom.xml
- 路径：`server/pom.xml`
- 功能：修改maven-compiler-plugin配置，使其兼容Java 8
- 修改内容：
  - 添加<source>8</source>和<target>8</target>配置
  - 添加<encoding>UTF-8</encoding>配置

## 5. 编译结果
- 编译命令：`mvn compile`
- 结果：成功

## 6. 注意事项
- 单点登录功能默认未启用，需要在配置文件中设置`sso.enabled=true`才能启用
- 启用后需要确保Redis服务器配置正确，否则会影响单点登录功能
- 系统现有的Redis配置（cache.type=redis）不会影响单点登录功能，因为单点登录使用独立的Redis配置
